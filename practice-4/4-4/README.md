# Выводы

VmSize показывает общий объём виртуальной памяти процесса.

VmRSS показывает объём памяти процесса,
который реально находится в оперативной памяти.

В эксперименте:

- VmSize = 2684 kB
- VmRSS = 1192 kB

Следовательно, не вся выделенная виртуальная память
физически загружена в RAM.
